import { enUS as commons } from './translations.commons';
import { enUS as pageTest } from '@pages/page-test/page-test.translations';

export const en = {
  translations: {
    commons,
    pages: {
      pageTest,
    },
  },
};
