import { ptBR as commons } from './translations.commons';
import { ptBR as pageTest } from '@pages/page-test/page-test.translations';

export const pt = {
  translations: {
    commons,
    pages: {
      pageTest,
    },
  },
};
